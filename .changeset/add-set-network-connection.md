---
"@samsara-dev/appwright": minor
---

Add `setNetworkConnection` method to Device class for controlling Android network connectivity via Appium's `mobile: setConnectivity` command. Supports toggling wifi, mobile data, and airplane mode.
